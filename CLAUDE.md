# Claude Code compatibility adapter

@AGENTS.md

Canonical project instructions live in `AGENTS.md`. Project skills and role
playbooks are stored once under `.agents/`; `.claude/` contains discovery adapters.
